/**
 * Stub — OpenAI has been replaced by the Anthropic Replit integration.
 * This file is kept so eval/test files that reference it still compile;
 * the actual LLM calls all go through anthropicClient.ts now.
 */
export const OPENAI_MODEL = "deprecated";

export const openai = null as never;
